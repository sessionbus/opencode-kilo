# OpenCode product facts

> Historical source note: citations to pre-split Sessionbus paths resolve in
> the Forgejo `ai/sessionbus` repository through its `legacy-*` branches.
> Citations to product source resolve in the external repository and full
> commit recorded by the split archive manifest. Host evidence paths are
> immutable external artifacts, not repository paths.

> Mandatory-wake source update (2026-09-21): OpenCode 1.18.30
> `noReply:true` persists a user message but returns without entering `loop()`;
> it therefore cannot prove active-lane consumption across a terminal race.
> Every lane delivery now returns NotRunning before the POST and daemon v0.5.7
> starts or schedules a normal managed Run. The interactive plugin keeps its
> bounded FIFO, answers active retention immediately, and submits a full native
> prompt on the next idle witness. See
> [the all-product boundary](https://github.com/antst/sessionbus-peers/blob/710e5d33369cba4fb9468cd24fea0fe844a0219d/docs/designs/mandatory-message-wake-20260921/NATIVE-BOUNDARIES.md).

## Current rewrite scope

The Go launcher/lane worker, native Bun hooks, ordinary activation and installer
are documented in [the current OpenCode README](../../opencode/README.md).
[Implementation and costs](../designs/opencode-0.5.0/IMPLEMENTATION.md) and
[the acceptance index](../designs/opencode-0.5.0/ACCEPTANCE.md) distinguish source
review, actual installed observations, failures and accepted limits.

The September 11 installed composition established native-only quiet resume,
confirmed title/groups and an actual generic list call on native 1.18.30. It
also established a cross-kind restriction: a retained Sessionbus lane ID cannot
register as an interactive Peer, even after close. Opening the same native
history does not reclassify it. This is separate from successful native-only
interactive resume. The first default-model parent-child collection failed and
is retained without an invented cause. Subsequent explicit-model parent/lane
composition passed with actual tools, completion pointers and complete answers.
The final selected rows also establish native old-session/child attribution
across TUI navigation, queued and idle delivery, exact lane resume/seed,
interruption after a native operation longer than five minutes, and scoped
normal/TERM/HUP/KILL cleanup. The acceptance index preserves raw evidence and
its limits, including the separate KILL stale-directory operator cleanup.

## Retained earlier product evidence

The following fact list preserves earlier implementations and probes, including
their contradictions and unverified statements. References below to "current"
describe those captured revisions, not the rewrite's selected runtime. In
particular, old v2 loop/bootstrap retries and installation dependencies are not
the new lane/installer design. Runtime evidence was captured on the dedicated
`umka-dev1` test host under
`/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z`.

## Version and process surface

- The installed product is `opencode-ai 1.18.29`, and the installed `@opencode-ai/plugin` is the matching `1.18.29`; the executable resolves to `/home/antst/.local/bin/opencode`. — verified: OpenCode 1.18.29 — source: `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P0-provenance/versions-after-update.txt`
- The isolated headless server script invoked `opencode serve --hostname 127.0.0.1 --port <port> --pure`; the evidence does not independently verify its process cwd. — verified: OpenCode 1.18.29 argv; process cwd unverified — source: `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/run/start-server.sh`
- The server accepts HTTP Basic authentication through `OPENCODE_SERVER_USERNAME` and `OPENCODE_SERVER_PASSWORD`; unauthenticated `/doc` returned 401 with a Basic challenge and the authenticated request returned 200. — verified: OpenCode 1.18.29 — source: `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P1-server/doc-unauth.raw`, `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P1-server/doc-auth.raw`
- Authentication isolation copied only `~/.local/share/opencode/auth.json` into the probe HOME; its contents were never captured and the copy was deleted at teardown. — verified: probe procedure — source: `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P0-provenance/auth-files-copied.txt`, `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P8-teardown/result.txt`
- Authenticated `/session` and `/event` both return 200; `/event` is an SSE stream whose first captured event was `server.connected`. — verified: OpenCode 1.18.29 — source: `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P1-server/session-auth.raw`, `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P1-server/event-auth.raw`
- `/doc` serves a 478,968-byte current OpenAPI document with 162 paths; the old 1.18.25 minimal `{"paths":{"/session":{},"/event":{}}}` test fixture is not the product response. — verified: OpenCode 1.18.29; contradiction: 1.18.25 pin fixture — source: `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P1-server/openapi.json`, `ff81565:internal/products/opencodefamily/client_test.go:295-312`
- Legacy session HTTP routes carry the absolute working directory in the `directory` query parameter. Every native request also needs `x-opencode-directory` set to that working directory: on the same OpenCode process, authenticated `/doc` without the header timed out after 8.027 seconds, while the header-bearing request returned its 478,968-byte document in 1.007 seconds. — verified: OpenCode 1.18.29, zero turns — source: `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P1-server/session-route-summary.json`, `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P2-crud/create-response.raw`, `/home/antst/sessionbus-evidence/opencode-20260906T233741Z/real/L0-open-resume/header-probe-2/result.json`
- On a fresh plugin-loading server, the first correctly scoped directory request remains pending until its caller cancels it. A first `/doc` attempt was cancelled after 4.024 seconds and an identical retry returned in 0.981 seconds; a first `/event` subscription likewise produced no event for 20.024 seconds, and after cancellation `/doc` returned in 1.056 seconds. Lane readiness therefore owns one five-second `bootstrapProbe` attempt inside the open context, cancels and drains it, then retries normally; child exit and the outer open cancellation still terminate readiness. — verified: OpenCode 1.18.29, zero turns — source: `/home/antst/sessionbus-evidence/opencode-20260906T233741Z/real/L0-open-resume/same-process-retry/result.json`, `/home/antst/sessionbus-evidence/opencode-20260906T233741Z/real/L0-open-resume/event-first-probe/result.json`
- Sessionbus lane sockets live beside the configured daemon socket under `lanes/<32-hex>.sock`; presence uses `run/presence.sock`. An explicit `SESSIONBUS_SOCKET` path must leave room for these suffixes within `sockaddr_un.sun_path`, whose limit is 108 bytes on Linux and 104 bytes on Darwin. — verified: harness observation and platform contract — source: `/home/antst/sessionbus-evidence/opencode-20260907T191503Z/dry-run/L6-lane/RESULT.txt`
- On Linux, `/proc/<pid>/comm` is limited to 15 bytes. Harness process inventory therefore requires the daemon binary basename and the exact `comm` predicate to be identical and no longer than 15 bytes. — verified: zero-turn diagnostic — source: `/home/antst/sessionbus-evidence/opencode-20260907T202225Z/probe/L6-peer-hello/RESULT.txt`, `/home/antst/sessionbus-evidence/opencode-20260907T203605Z/probe/L6-peer-hello/RESULT.txt`
- The default invocation is `opencode [project]` and starts the interactive TUI. Its captured managed selectors are `-m/--model <string>`, `-c/--continue`, `-s/--session <string>`, `--fork`, `--prompt <string>`, `--agent <string>`, `--auto`, `--mini`, `--no-replay`, and `--replay-limit <number>`; shared options include boolean `--print-logs`/`--pure`/`--mdns`, string `--log-level`/`--hostname`/`--mdns-domain`, numeric `--port`, and array `--cors`. — verified: OpenCode 1.18.29 — source: `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P0-provenance/opencode-help.err`
- The probe used `serve --pure` only for isolation; the captured help defines `--pure` as disabling external plugins, so a plugin-native worker or peer launch must omit it or the sessionbus plugin cannot load. — verified: OpenCode 1.18.29 — source: `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P0-provenance/opencode-help.err`
- OpenCode selects its writable global config as the first existing file in `opencode.jsonc`, `opencode.json`, `config.json` order and defaults to `opencode.jsonc` when none exists. — verified: OpenCode 1.18.29 source — source: `1674747:packages/opencode/src/config/config.ts:140-148`
- OpenCode loads and merges every global config file in `config.json`, `opencode.json`, `opencode.jsonc` order; an installer must therefore remove its owned entry from every existing merged file before writing one entry to the selected file. — verified: OpenCode 1.18.29 source — source: `1674747:packages/opencode/src/config/config.ts:260-274`
- Each configured plugin is either a package-specifier string or a `[specifier, options]` tuple. — verified: OpenCode 1.18.29 source — source: `1674747:packages/core/src/v1/config/plugin.ts:5-9`
- The config `plugin` array in OpenCode 1.18.29 resolved and loaded each tested exact form: `file:<absolute package directory>`, `file:<absolute npm tarball>`, a loopback HTTP tarball URL, and the exact npm package `opencode-models-discovery@1.5.3`. The three fixture forms executed the same constructor marker; the npm package emitted its own initialization marker. — verified: OpenCode 1.18.29, zero turns — source: `/home/antst/sessionbus-evidence/opencode-plugin-spec-20260906T205328Z/file-directory/marker.txt`, `/home/antst/sessionbus-evidence/opencode-plugin-spec-20260906T205328Z/file-tarball/marker.txt`, `/home/antst/sessionbus-evidence/opencode-plugin-spec-20260906T205328Z/http-tarball/marker.txt`, `/home/antst/sessionbus-evidence/opencode-plugin-spec-20260906T205328Z/http.stderr`, `/home/antst/sessionbus-evidence/opencode-plugin-spec-20260906T205328Z/npm-exact/data/opencode/log/opencode.log`
- Starting `opencode serve` did not itself execute a configured plugin constructor. Querying `/experimental/tool/ids?directory=<case-root>` created the native instance and loaded it; this is the same product-owned observation used by the lane readiness barrier. — verified: OpenCode 1.18.29, zero turns — source: `/home/antst/sessionbus-evidence/opencode-plugin-spec-20260906T205328Z/RUN-SUMMARY.md`, `/home/antst/sessionbus-evidence/opencode-plugin-spec-20260906T205328Z/file-directory/tool-ids.raw`, `/home/antst/sessionbus-evidence/opencode-plugin-spec-20260906T205328Z/file-directory/data/opencode/log/opencode.log`

## Open-field inventory

- `cwd` is product-verified as the `directory` scope on legacy session routes and as the directory returned on the created native session; the server process cwd was not independently captured. — verified: OpenCode 1.18.29 session scope; process cwd unverified — source: `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P1-server/session-route-summary.json`, `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P2-crud/create-response.raw`
- The legacy v1 prompt model object uses `providerID` and `modelID`. The v2 model-switch route used by the wrapper instead requires `{model:{id,providerID,variant?}}`; the current CLI accepts `-m/--model` as one `provider/model` string, which the wrapper splits into the v2 `providerID` and `id`. — verified: OpenCode 1.18.29 generated type and runtime rejection of the v1 field on the v2 route — source: `1674747:packages/sdk/js/src/v2/gen/types.gen.ts:3041-3045`, `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P5-surface/session-turn-paths.json`, `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P0-provenance/opencode-help.err`, `/home/antst/sessionbus-evidence/opencode-20260906T233741Z/real/L0-bootstrap-fixed/open.json`
- UNVERIFIED: the shared `permission_mode` mapping on 1.18.29. The old adapter mapped absent/default to one wildcard `ask` rule, `bypassPermissions` to one wildcard `allow` rule, and rejected every other mode. — old adapter, unverified on OpenCode 1.18.29 — source: `ff81565:internal/products/opencodefamily/permission.go:8-20`
- UNVERIFIED: the shared `reasoning_effort` mapping on 1.18.29. The old lane copied effort into the native prompt `variant`; the current OpenAPI proves only that `variant` is an optional string and does not prove accepted shared values or their meaning. — old adapter plus current schema, unverified runtime mapping — source: `ff81565:internal/products/opencodefamily/lane.go:178-184`, `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P5-surface/session-turn-paths.json`
- Native `arguments` must follow the captured CLI arities above. The old lane consumed `-m/--model` and `--agent` as typed prompt controls and passed other safe server arguments onward; `--agent` is a verified one-string current flag, but the complete wrapper policy remains to be derived from the 1.18.29 fixture rather than guessed. — verified current flag surface; old adapter mapping — source: `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P0-provenance/opencode-help.err`, `ff81565:internal/products/opencodefamily/lane.go:215-255`

## Session lifecycle and frames

- Fresh creation is `POST /session?directory=<cwd>` with a body that can contain a title and permission rules; the captured response returned a native `ses_*` ID, the requested title, directory, version, zeroed cost/tokens, permission rules, and timestamps. — verified: OpenCode 1.18.29 — source: `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P2-crud/create-request.json`, `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P2-crud/create-response.raw`
- `GET /session/<ses_id>?directory=<cwd>` returned the exact created session, including the same ID and title. — verified: OpenCode 1.18.29 — source: `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P2-crud/get-response.raw`
- Native retitle is `PATCH /session/<ses_id>?directory=<cwd>` with `{"title":"<title>"}`; the captured response repeated the exact ID and new title. — verified: OpenCode 1.18.29 — source: `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P2-crud/patch-request.json`, `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P2-crud/patch-response.raw`
- Native deletion is `DELETE /session/<ses_id>?directory=<cwd>`; both captured deletions returned exact JSON `true`. — verified: OpenCode 1.18.29 — source: `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P2-crud/delete-response.raw`, `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P8-teardown/P3-turn1-delete.body`
- Current SSE lifecycle frames have a top-level event `id`, a `type`, and `properties.sessionID`; `session.created`, `session.updated`, and `session.deleted` also carry full session data in `properties.info`. — verified: OpenCode 1.18.29 — source: `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P2-crud/lifecycle-events.jsonl`
- The current OpenAPI constrains native session, message, and permission IDs only by their `ses`, `msg`, and `per` prefixes; the tighter 256-byte ASCII grammar in the old adapter is not a documented 1.18.29 product contract. — verified: OpenCode 1.18.29; contradiction: 1.18.25 adapter validation — source: `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P1-server/openapi.json`, `ff81565:internal/products/opencodefamily/client.go:25-29`
- Legacy TUI turns use `POST /session/<ses_id>/prompt_async?directory=<cwd>`; the synchronous sibling is `POST /session/<ses_id>/message?directory=<cwd>`. Both accept the same prompt body: required `parts`, with optional `messageID`, `model`, `agent`, `noReply`, `tools`, `format`, `system`, and `variant`. The synchronous route returns the created message, while `prompt_async` starts the session asynchronously and returns 204. — verified: OpenCode 1.18.29 source and OpenAPI — source: `1674747:packages/opencode/src/server/routes/instance/httpapi/groups/session.ts:316-341`, `1674747:packages/opencode/src/server/routes/instance/httpapi/groups/session.ts:70`, `1674747:packages/opencode/src/session/prompt.ts:1494-1521`, `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P5-surface/session-turn-paths.json`
- The captured `prompt_async` request used deterministic ID `msg_opencode_probe_turn_1`, agent `build`, model `{providerID:"opencode",modelID:"big-pickle"}`, and one text part. Acceptance was HTTP 204 with an empty body, and the legacy transcript completed with `OC_DONE`. — verified: OpenCode 1.18.29 — source: `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P3-turn1/prompt-request.json`, `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P3-turn1/prompt.headers`, `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P3-turn1/prompt.body`, `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P3-turn1/messages-final.json`
- The terminal transcript from `GET /session/<ses_id>/message` preserved the admitted user message before its assistant child; the assistant carried `time.completed`, `finish:"stop"`, and the text `OC_DONE`. — verified: OpenCode 1.18.29 — source: `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P3-turn1/messages-final.json`
- Ascending v2 message history can begin with `model-switched` and `agent-switched` pseudo-messages before the first user/assistant pair. Transcript assertions must filter by role rather than assume every history item is a conversational message. — verified: OpenCode 1.18.29 runtime — source: `/home/antst/sessionbus-evidence/opencode-20260907T141343Z/real/L3/native-message-history.json`
- A TUI tool turn ran entirely in the legacy event family: the legacy stream and pane contained the prompt, one `sessionbus` tool call, `tool-calls`, and final stop, while `/api/session/<id>/message` remained empty and the per-session durable sequence remained zero. Interactive-TUI assertions must use the legacy stream rather than v2 history. — verified: OpenCode 1.18.29 runtime — source: `/home/antst/sessionbus-evidence/opencode-20260907T154625Z/real/P1/legacy-events.raw`, `/home/antst/sessionbus-evidence/opencode-20260907T154625Z/real/P1/native-message-history-final.json`, `/home/antst/sessionbus-evidence/opencode-20260907T154625Z/real/P1/native-durable-bound.json`, `/home/antst/sessionbus-evidence/opencode-20260907T154625Z/real/P1/tui-pane-failure.txt`
- A legacy run publishes `session.status` with `type:"busy"` while active, then `session.status` with `type:"idle"` and deprecated `session.idle` at its boundary. `SessionRunState` installs those busy/idle transitions, and `SessionStatus.set` publishes both the status and the extra idle event before deleting idle state. — verified: OpenCode 1.18.29 legacy runtime and source — source: `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P3-turn1/session-events.jsonl`, `1674747:packages/opencode/src/session/run-state.ts:52-68`, `1674747:packages/opencode/src/session/status.ts:39-47`
- The TUI subscribes to the product event source and projects legacy `message.updated`, `message.part.updated`, and `session.status` into the selected session's message, part, and status stores; the session route renders from those stores. A message submitted through legacy `prompt_async` is therefore presented in the TUI. — verified: OpenCode 1.18.29 source and P1 runtime — source: `1674747:packages/tui/src/context/event.ts:9-35`, `1674747:packages/tui/src/context/sync.tsx:176-180,316-395`, `1674747:packages/tui/src/routes/session/index.tsx:185-213,327-360`, `/home/antst/sessionbus-evidence/opencode-20260907T154625Z/real/P1/legacy-events.raw`, `/home/antst/sessionbus-evidence/opencode-20260907T154625Z/real/P1/tui-pane.txt`
- The v2 `POST /api/session/{sessionID}/wait` route is unusable in 1.18.29: its OpenAPI advertises an idle wait and a 204 result, but `SessionV2.wait` unconditionally returns `OperationUnavailableError`, and an idle live session returned HTTP 503 with `Session wait is not available yet`. The wrapper never calls or retries this route. — verified: OpenCode 1.18.29 source and zero-turn runtime diagnostic — source: `1674747:packages/core/src/session.ts:421-424`, `https://github.com/anomalyco/opencode/issues/33605`, `/home/antst/sessionbus-evidence/opencode-20260907T074243Z/real/L1-wait-diagnostic/doc-wait.json`, `/home/antst/sessionbus-evidence/opencode-20260907T074243Z/real/L1-wait-diagnostic/idle-wait-response.json`
- In the first Sessionbus L1 run, the v2 prompt was admitted and a concurrent second `turn.run` correctly returned `busy`; `/wait` then produced the known 503, so the pre-fix wrapper failed while OpenCode continued. The later v2 history contained the exact admitted user message, one completed `tool-calls` assistant, and a completed `stop` assistant with `OPENCODE_RUN_OK`. — verified: OpenCode 1.18.29, one model turn — source: `/home/antst/sessionbus-evidence/opencode-20260907T074243Z/real/L1-run-busy/run.json`, `/home/antst/sessionbus-evidence/opencode-20260907T074243Z/real/L1-run-busy/second.json`, `/home/antst/sessionbus-evidence/opencode-20260907T074243Z/real/L1-run-busy/native-history-diagnostic.json`
- The stamped SSE-idle follow-up repeated the same v2 prompt successfully: native history completed with `OPENCODE_RUN_OK`, while the wrapper remained `running:true` for more than 90 seconds because no matching `session.idle` reached the run. Bounded close reaped the wrapper and native process. Exact 1.18.29 source explains the result: v2 uses `SessionRunCoordinator`, which publishes no status/idle event, and its durable event inventory contains step ended/failed boundaries but no execution-ended boundary. — verified: OpenCode 1.18.29 source and one model turn — source: `/home/antst/sessionbus-evidence/opencode-20260907T074243Z/real/L1-rerun/RUN-SUMMARY.md`, `/home/antst/sessionbus-evidence/opencode-20260907T074243Z/real/L1-rerun/native-history-hung.json`, `/home/antst/sessionbus-evidence/opencode-20260907T074243Z/real/L1-rerun/roster-hung.json`, `1674747:packages/core/src/session/execution/local.ts:16-35`, `1674747:packages/schema/src/session-event.ts:448-510`
- Captured v2 `session.next.step.ended` carries `sessionID`, `assistantMessageID`, and `finish` under top-level `data`, not legacy `properties`; the exact captured terminal frame had `finish:"stop"`. Legacy `permission.asked` retains `sessionID` and request `id` under `properties`. — verified: OpenCode 1.18.29 runtime — source: `/home/antst/agentbus-evidence/opencode-probes-3-20260906T180942Z/T1-v2-promotion/events-v2.raw`, `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P4-permission/events.raw`
- One turn captured both event families: legacy `/event` carried `message.*`, `session.status`, and `session.idle` but no `session.next.*`; its durable step frames arrived on `GET /api/session/<sessionID>/event`. The wrapper therefore keeps legacy `/event` only for permission events and opens the per-session durable stream for step boundaries after the session ID exists. — verified: OpenCode 1.18.29 runtime — source: `/home/antst/agentbus-evidence/opencode-probes-2-20260906T175921Z/T2-queue/events-legacy.raw`, `/home/antst/agentbus-evidence/opencode-probes-3-20260906T180942Z/T1-v2-promotion/events-v2.raw`, `/home/antst/agentbus-evidence/opencode-probes-3-20260906T180942Z/T1-v2-promotion/run-turn.sh:5-7`
- The per-session durable route is `v2.session.events` at `/api/session/:sessionID/event` and optionally replays after `?after=<seq>`; the wrapper opens it without `after`. Replayed earlier steps on a resumed session are harmless because terminal correlation rejects assistants before the current admitted user. `/api/event` is the separate server-wide v2 stream and is not used by this wrapper. — verified: OpenCode 1.18.29 source-derived wrapper contract — source: `1674747:packages/protocol/src/groups/session.ts:327-341`, `1674747:packages/protocol/src/groups/event.ts:35`, `1674747:packages/opencode/src/server/routes/instance/httpapi/groups/event.ts`
- An interrupt during an active assistant produced byte-exact durable `session.next.step.failed` with top-level `data.timestamp`, `sessionID`, `assistantMessageID`, and `error:{type:"unknown",message:"Provider turn interrupted"}`. The wrapper fixture is copied from that sealed frame, not reconstructed from the schema. — verified: OpenCode 1.18.29 runtime — source: `/home/antst/sessionbus-evidence/opencode-20260907T120702Z/real/step-failed-interrupt/captured-step-failed.raw`
- An unknown v2 `ModelRef` was admitted and prompted, then model resolution failed before `step.started`; the drain ended without any step boundary or assistant in history. The local execution layer logs non-interrupt drain failures and the coordinator settles its entry without publishing a terminal event, so this is a third no-boundary ending beside final-step tool calls and an unanswered question. This unit does not add a model precheck; caller Interrupt is the escape. — product gap: OpenCode 1.18.29 runtime and source — source: `/home/antst/sessionbus-evidence/opencode-20260907T120702Z/real/step-failed-capture/native-durable-replay.raw`, `/home/antst/sessionbus-evidence/opencode-20260907T120702Z/real/step-failed-capture/native-message-history.json`, `1674747:packages/core/src/session/runner/llm.ts:180-203`, `1674747:packages/core/src/session/execution/local.ts:16-29`, `1674747:packages/core/src/session/run-coordinator.ts:45-64`
- `failAssistant` publishes `session.next.step.failed` with `error.type:"unknown"`. Its callers are provider-error stream events, LLM stream failures, and interruption while an assistant is active; the interrupt path uses message `Provider turn interrupted`. — verified: OpenCode 1.18.29 source and runtime — source: `1674747:packages/core/src/session/runner/publish-llm-event.ts:199-210`, `1674747:packages/core/src/session/runner/publish-llm-event.ts:404-407`, `1674747:packages/core/src/session/runner/llm.ts:294-316`, `/home/antst/sessionbus-evidence/opencode-20260907T120702Z/real/step-failed-interrupt/native-durable-replay.raw`
- Durable publication runs every projector in the same transaction before inserting the event row; the step-ended projector sets that assistant's `time.completed` and `finish`. History therefore already contains the completed assistant when the stream delivers its step event, so one history read is sufficient and there is no history-lag retry. — verified: OpenCode 1.18.29 source — source: `1674747:packages/core/src/event.ts:320-322`, `1674747:packages/core/src/session/message-updater.ts:209-215`
- The v2 terminal contract is event-owned and never consults `/api/session/active`: an exact-session `session.next.step.ended` with `finish:"stop"` completes this run only when its `assistantMessageID` identifies a completed assistant after the exact admitted user in history; an uncorrelated stop waits for the next step event. A `session.next.step.failed` returns its native error unless history already contains the completed stop, and every other step finish waits. The coordinator retains active membership for the whole drain and removes it only after the runner returns, while `step.ended` is published before the runner decides whether to continue, so `/active` cannot classify the event that triggered the read. — verified: OpenCode 1.18.29 source-derived wrapper contract — source: `1674747:packages/core/src/session/run-coordinator.ts:37-64`, `1674747:packages/core/src/session/runner/llm.ts:323-352`, `1674747:packages/core/src/session/runner/llm.ts:390-412`
- A successful wrapper-owned permission rejection or v2 interrupt reconciles history once without waiting for another native event. A completed stop after the admitted user wins; otherwise the wrapper reports `OpenCode run ended by permission rejection` or `OpenCode run ended by interrupt`. The action is preserved alongside an already-buffered native terminal rather than replacing or dropping either signal. Resolution is deterministic: correlated stop wins, then the wrapper action, then a native failure without an action. An active v2 interrupt causes OpenCode to publish `step.failed` after the action; that failure is the consequence of the action, so the Run reports `OpenCode run ended by interrupt` regardless of subscriber timing. OpenCode permission rejection interrupts the runner before `Step.Ended` is published, so the action is itself a necessary boundary when no stop completed first. — verified: wrapper contract, OpenCode 1.18.29 source, and runtime — source: `1674747:packages/core/src/session/runner/llm.ts:302-316`, `1674747:packages/core/src/session/run-coordinator.ts:94-101`, `/home/antst/sessionbus-evidence/opencode-20260907T120702Z/real/step-failed-interrupt/run.json`, `/home/antst/sessionbus-evidence/opencode-20260907T120702Z/real/step-failed-interrupt/native-durable-replay.raw`
- An uncovered terminal gap occurs when an agent reaches its configured final step but the provider still returns tool calls: tools are unavailable, `needsContinuation` remains false, the step can end with `finish:"tool-calls"`, and the drain returns without a later boundary event. The 1.18.29 `/wait` route is unavailable, so the wrapper neither polls nor times out; caller Interrupt is the escape. — product gap: OpenCode 1.18.29 — source: `1674747:packages/core/src/session/runner/llm.ts:202-220`, `1674747:packages/core/src/session/runner/llm.ts:250-255`, `1674747:packages/core/src/session/runner/llm.ts:323-352`, `1674747:packages/core/src/session/runner/llm.ts:400-412`, `1674747:packages/core/src/session.ts:421-424`
- A v2 `question.v2.asked` left unanswered is another uncovered behavior: the question tool awaits its deferred reply, so the execution remains open and emits no terminal step boundary. This wrapper does not answer or reject questions in the current unit; caller Interrupt is the escape. — product gap: OpenCode 1.18.29 — source: `1674747:packages/schema/src/question.ts:70-85`, `1674747:packages/core/src/question.ts:93-107`, `1674747:packages/core/src/tool/question.ts:62-83`
- A wildcard `ask` rule produced `permission.asked` with exact fields `id`, `sessionID`, `permission:"bash"`, `patterns`, `metadata.command`, `always`, and nested tool message/call IDs. — verified: OpenCode 1.18.29 — source: `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P4-permission/permission-event.json`
- Permission response is `POST /session/<ses_id>/permissions/<per_id>?directory=<cwd>` with `{"response":"once|always|reject"}`; the captured reject returned exact JSON `true` and emitted `permission.replied`. — verified: OpenCode 1.18.29 — source: `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P4-permission/permission-response-request.json`, `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P4-permission/permission-response.body`, `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P4-permission/session-events.jsonl`
- After permission rejection, the transcript's tool part had `state.status:"error"` and error text `The user rejected permission to use this specific tool call.`; the assistant had `time.completed` and `finish:"tool-calls"`. — verified: OpenCode 1.18.29; contradiction: the 1.18.25 adapter classified every `tool-calls` finish as nonterminal — source: `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P4-permission/messages-final.json`, `ff81565:internal/products/opencodefamily/client.go:377-381`
- Interrupt is `POST /session/<ses_id>/abort?directory=<cwd>` with no documented request body; an idle-session probe returned exact JSON `true`. — verified: OpenCode 1.18.29 — source: `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P5-surface/session-turn-paths.json`, `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P2-crud/abort-response.raw`
- UNVERIFIED: the response, events, transcript, and terminal outcome when abort is sent during an active 1.18.29 turn; only the idle `true` response was captured. — unverified: OpenCode 1.18.29 — source: idle evidence above
- The v2 interrupt surface is `POST /api/session/{sessionID}/interrupt` with no request body and HTTP 204 success; its current OpenAPI says it interrupts active execution owned by this OpenCode process and is an idle no-op. — verified: OpenCode 1.18.29 OpenAPI — source: `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P5-surface/v2-session-paths.json`
- During an active assistant, the v2 interrupt route returned HTTP 204, the durable stream published `step.failed` with `Provider turn interrupted`, and projected history completed that assistant with `finish:"error"`. Because the wrapper action is the deterministic terminal authority for its own interrupt, Sessionbus returned outcome `failed` and exact result `OpenCode run ended by interrupt`. — verified: OpenCode 1.18.29 runtime — source: `/home/antst/sessionbus-evidence/opencode-20260907T120702Z/real/step-failed-interrupt/interrupt.json`, `/home/antst/sessionbus-evidence/opencode-20260907T120702Z/real/step-failed-interrupt/native-durable-replay.raw`, `/home/antst/sessionbus-evidence/opencode-20260907T120702Z/real/step-failed-interrupt/native-message-history.json`, `/home/antst/sessionbus-evidence/opencode-20260907T120702Z/real/step-failed-interrupt/run.json`
- Exact-ID resume returned the same native session ID in five consecutive zero-turn retries, each before the named 45-second bound. An earlier resume of that same session timed out once after 45 seconds and remains an observed, unexplained event; a later diagnostic did not reproduce it and mistakenly captured post-open idle stacks. — verified equality; one unreproduced timeout: OpenCode 1.18.29 — source: `/home/antst/sessionbus-evidence/opencode-20260907T134337Z/real/L0-open-resume`, `/home/antst/sessionbus-evidence/opencode-20260907T135312Z/diagnostic/L0-resume/RESULT-AMEND.txt`, `/home/antst/sessionbus-evidence/opencode-20260907T140658Z/diagnostic/L0-resume-attempt-5/L0-SUMMARY.txt`
- UNVERIFIED: the exact 1.18.29 `session.error` frame and failed-turn transcript; the old nested `UnknownError.data.message` fixture is pin evidence only. — unverified: OpenCode 1.18.29 — source: `ff81565:internal/products/opencodefamily/client_test.go:277-292`
- UNVERIFIED: whether deleting an already absent session is treated as converged; only successful deletion was exercised. — unverified: OpenCode 1.18.29 — source: successful evidence `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P2-crud/delete-response.raw`

## Native delivery and MCP surfaces

- The v2 product API exposes `POST /api/session/{sessionID}/prompt`; its documented body is `{id?,prompt,delivery?,resume?}`, and `delivery` is exactly `"steer"|"queue"`. — verified: OpenCode 1.18.29 OpenAPI — source: `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P5-surface/v2-session-paths.json`
- The v2 prompt route is documented to durably admit one session input and schedule agent-loop execution unless `resume` is false; success returns `SessionInputAdmitted` with `admittedSeq`, exact message/session IDs, the prompt, the selected `delivery`, `timeCreated`, and optional `promotedSeq`. — verified: OpenCode 1.18.29 OpenAPI — source: `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P5-surface/v2-prompt-schemas.json`, `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P5-surface/v2-session-paths.json`
- `/tui/append-prompt` accepts only `{text}` and is documented as appending prompt text to the TUI; it is not a session-addressed delivery operation. — verified: OpenCode 1.18.29 OpenAPI — source: `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P5-surface/matching-paths.json`
- No other route containing append, inject, interject, or queue appears in the 1.18.29 OpenAPI; the SDK exposes legacy `promptAsync`, v2 prompt delivery, and TUI `appendPrompt`. — verified: OpenCode 1.18.29 — source: `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P5-surface/matching-paths.txt`, `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P5-surface/sdk-method-grep.txt`
- During a legacy `prompt_async` turn's running bash tool, `POST /api/session/{sessionID}/prompt` with `delivery:"steer"` returned `SessionInputAdmitted` with `admittedSeq:14`, the exact message/session IDs and prompt, `delivery:"steer"`, and no `promotedSeq`. — verified: OpenCode 1.18.29 — source: `/home/antst/agentbus-evidence/opencode-probes-2-20260906T175921Z/T1-steer/steer-response.json`, `/home/antst/agentbus-evidence/opencode-probes-2-20260906T175921Z/T1-steer/active-tool-event.json`
- That accepted steer emitted `session.next.prompt.admitted` followed by `session.next.prompted` and produced a separate v2 user/assistant transcript containing `STEER_SEEN`; the concurrently running legacy transcript did not contain the steer input and completed independently with `FIRST_DONE`. Native v2 steer across a legacy turn is therefore a distinct agent loop, not same-legacy-turn injection. — verified: OpenCode 1.18.29 — source: `/home/antst/agentbus-evidence/opencode-probes-2-20260906T175921Z/T1-steer/session-events.jsonl`, `/home/antst/agentbus-evidence/opencode-probes-2-20260906T175921Z/T1-steer/messages-v2.json`, `/home/antst/agentbus-evidence/opencode-probes-2-20260906T175921Z/T1-steer/messages-legacy.json`
- An idle v2 admission with `delivery:"queue"` and `resume:false` returned `admittedSeq:1`, no `promotedSeq`, started no model, and remained absent from both legacy and v2 transcripts. — verified: OpenCode 1.18.29 — source: `/home/antst/agentbus-evidence/opencode-probes-2-20260906T175921Z/T2-queue/queue-response.json`, `/home/antst/agentbus-evidence/opencode-probes-2-20260906T175921Z/T2-queue/messages-legacy-before-restart.json`, `/home/antst/agentbus-evidence/opencode-probes-2-20260906T175921Z/T2-queue/messages-v2-before-restart.json`
- The queued admission survived an orderly server stop/join and restart as the exact durable `session.next.prompt.admitted` event at aggregate sequence 1; the session itself also survived with the same ID and title. — verified: OpenCode 1.18.29 — source: `/home/antst/agentbus-evidence/opencode-probes-2-20260906T175921Z/T2-queue/processes-between-servers.txt`, `/home/antst/agentbus-evidence/opencode-probes-2-20260906T175921Z/T2-queue/restart-result.txt`, `/home/antst/agentbus-evidence/opencode-probes-2-20260906T175921Z/T2-queue/history-after-restart.json`, `/home/antst/agentbus-evidence/opencode-probes-2-20260906T175921Z/T2-queue/session-after-restart.json`
- Submitting the next turn through legacy `prompt_async` did not promote or consume that v2 queue entry: the legacy turn completed with `NORMAL_MARK`, the v2 transcript stayed empty, and durable history still contained only the unpromoted sequence-1 admission. — verified: OpenCode 1.18.29 — source: `/home/antst/agentbus-evidence/opencode-probes-2-20260906T175921Z/T2-queue/messages-legacy-final.json`, `/home/antst/agentbus-evidence/opencode-probes-2-20260906T175921Z/T2-queue/messages-v2-final.json`, `/home/antst/agentbus-evidence/opencode-probes-2-20260906T175921Z/T2-queue/history-final.json`
- The legacy `prompt_async` route is documented to create and send a legacy message and start the session, while the v2 `/api/session/{sessionID}/prompt` route is documented to durably admit input with explicit delivery and resume controls; only the latter returns `SessionInputAdmitted`. — verified: OpenCode 1.18.29 OpenAPI — source: `/home/antst/agentbus-evidence/opencode-probes-2-20260906T175921Z/T2-queue/route-comparison.json`
- A queued v2 input survived an orderly server stop/join and restart, then a later v2 `/prompt` submission returned `admittedSeq:2` with defaulted `delivery:"steer"` and no `promotedSeq`. OpenCode completed that new prompt first as `V2_NORMAL`, emitted `session.next.prompted` for the older queued sequence 1, and completed it as `QUEUE_PROMOTED`; the v2 transcript records the same order. — verified: OpenCode 1.18.29 — source: `/home/antst/agentbus-evidence/opencode-probes-3-20260906T180942Z/T1-v2-promotion/processes-between.txt`, `/home/antst/agentbus-evidence/opencode-probes-3-20260906T180942Z/RUN-SUMMARY.md`, `/home/antst/agentbus-evidence/opencode-probes-3-20260906T180942Z/T1-v2-promotion/history-before-restart.json`, `/home/antst/agentbus-evidence/opencode-probes-3-20260906T180942Z/T1-v2-promotion/history-after-restart.json`, `/home/antst/agentbus-evidence/opencode-probes-3-20260906T180942Z/T1-v2-promotion/normal-response.json`, `/home/antst/agentbus-evidence/opencode-probes-3-20260906T180942Z/T1-v2-promotion/history-after-promotion.json`, `/home/antst/agentbus-evidence/opencode-probes-3-20260906T180942Z/T1-v2-promotion/messages-v2-after-promotion.json`
- A later same-family steer probe failed its active-state harness barrier after admitting the base v2 prompt. The one steer was therefore sent only after the base bash tool had completed with `finish:"tool-calls"`; OpenCode continued the base to `V2_BASE_DONE` before promoting the steer and completing a separate `STEER2_SEEN` response. — verified: OpenCode 1.18.29 ordering; active timing unverified — source: `/home/antst/agentbus-evidence/opencode-probes-3-20260906T180942Z/T2-v2-steer/run-turn.stderr`, `/home/antst/agentbus-evidence/opencode-probes-3-20260906T180942Z/T2-v2-steer/history-after-harness-failure.json`, `/home/antst/agentbus-evidence/opencode-probes-3-20260906T180942Z/T2-v2-steer/steer-response.json`, `/home/antst/agentbus-evidence/opencode-probes-3-20260906T180942Z/T2-v2-steer/history-after-steer.json`
- A successful `SessionInputAdmitted` response with `delivery:"steer"` is native acceptance: the session processes the admitted input without another prompt, after the current step. The Sessionbus receipt is therefore `injected` even though entry during an actively executing v2 model/tool step remains unverified. — verified: OpenCode 1.18.29 observed admission and processing — source: `/home/antst/agentbus-evidence/opencode-probes-3-20260906T180942Z/T2-v2-steer/steer-response.json`, `/home/antst/agentbus-evidence/opencode-probes-3-20260906T180942Z/T2-v2-steer/history-after-steer.json`
- During an active v2 run, `delivery:"steer"` was admitted before the current assistant ended its `tool-calls` step, then promoted at that next step boundary within the same execution. One continuation assistant followed and ended `stop`; a separate steer assistant and second stop are not guaranteed because that continuation answers with both the original and steered inputs in history. Sessionbus reported the native acceptance as `injected`, while the caller's Run projected only its correlated terminal assistant. — verified: OpenCode 1.18.29 runtime — source: `/home/antst/sessionbus-evidence/opencode-20260907T143438Z/real/L4/steer.json`, `/home/antst/sessionbus-evidence/opencode-20260907T143438Z/real/L4/run.json`, `/home/antst/sessionbus-evidence/opencode-20260907T143438Z/real/L4/native-durable-replay.raw`, `/home/antst/sessionbus-evidence/opencode-20260907T143438Z/real/L4/native-message-history.json`
- Idle peer delivery through legacy `prompt_async` returned `queued_for_next_turn`; the resumed TUI presented the delivered user message and completed one stop assistant. The legacy stream emitted byte-equivalent duplicate completed `message.updated` notifications for that assistant, so terminal matching deduplicates by assistant ID and correlates one user message to its completed stop assistant rather than matching model prose. — verified: OpenCode 1.18.29 runtime — source: `/home/antst/sessionbus-evidence/opencode-20260907T184742Z/real/P2-legacy/delivery.json`, `/home/antst/sessionbus-evidence/opencode-20260907T184742Z/real/P2-legacy/legacy-events.raw`, `/home/antst/sessionbus-evidence/opencode-20260907T184742Z/real/P2-legacy/tui-pane.txt`, `/home/antst/sessionbus-evidence/opencode-20260907T184742Z/real/P2-legacy/RESULT-AMEND.txt`
- Killing the native server after a lane run's first durable `step.started` made the old Run fail promptly with a truthful transport error and took the row offline. Exact-session resume started new product processes; a new Run was admitted, active delivery returned `injected`, and the resumed Run completed with stop. No wrapper turn state crossed the process boundary, while the resumed native session correctly retained its transcript. — verified: OpenCode 1.18.29 abnormal-end recovery — source: `/home/antst/sessionbus-evidence/opencode-20260907T191826Z/real/L6-lane/RESULT-AMEND.txt`, `/home/antst/sessionbus-evidence/opencode-20260907T191826Z/real/L6-lane/offline.json`, `/home/antst/sessionbus-evidence/opencode-20260907T191826Z/real/L6-lane/resume.json`, `/home/antst/sessionbus-evidence/opencode-20260907T191826Z/real/L6-lane/delivery.json`, `/home/antst/sessionbus-evidence/opencode-20260907T191826Z/real/L6-lane/native-durable-replay.raw`
- In that lane recovery, the model treated the delivered instruction as prompt injection and quoted it instead of obeying it. This is model behavior, not a wrapper-state failure: product gates assert receipts, message roles and parentage, and terminal structure, never compliance with or absence of model prose. — observed: OpenCode 1.18.29 model behavior — source: `/home/antst/sessionbus-evidence/opencode-20260907T191826Z/real/L6-lane/new-run.json`, `/home/antst/sessionbus-evidence/opencode-20260907T191826Z/real/L6-lane/durable-summary.json`
- Peer abnormal-end recovery did not repeat the already sealed killed turn. After exact-session reopen and announcement, a new TUI turn became busy; delivery returned `queued_for_next_turn` immediately, produced no native submission while busy, and caused exactly one `prompt_async` after the next idle boundary. The TUI then presented one delivered user message followed by its completed stop assistant, with no refusal. — verified: OpenCode 1.18.29 abnormal-end recovery — source: `/home/antst/sessionbus-evidence/opencode-20260907T204730Z/real/L6-peer/ADMISSION-AMEND.txt`, `/home/antst/sessionbus-evidence/opencode-20260907T210509Z/real/L6-peer-resume/delivery.json`, `/home/antst/sessionbus-evidence/opencode-20260907T210509Z/real/L6-peer-resume/no-submit-while-busy.txt`, `/home/antst/sessionbus-evidence/opencode-20260907T210509Z/real/L6-peer-resume/delivery-terminal.json`, `/home/antst/sessionbus-evidence/opencode-20260907T210509Z/real/L6-peer-resume/RESULT-AMEND.txt`
- Lane-wrapper criterion: `opencode-peer` runs and delivery use the v2 session family exclusively. Pairing a legacy `prompt_async` run with v2 steer or queue is invalid because the captured families do not consume each other's inputs. This constraint does not make v2 suitable for an interactive TUI peer, whose presented transcript remains in the legacy family. — verified design constraint from product evidence — source: probe-2, probe-3, and P2 facts above
- Native MCP configuration supports local stdio servers with `type:"local"`, command argv, cwd, environment, enablement, and timeout; it also supports remote URL servers with headers, OAuth configuration, enablement, and timeout. — verified: OpenCode 1.18.29 OpenAPI — source: `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P6-mcp/mcp-schemas.json`
- The current server exposes MCP status, add, connect, disconnect, auth, callback, and OAuth removal routes, and the CLI exposes `opencode mcp add|list|auth|logout|debug`. — verified: OpenCode 1.18.29 — source: `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P6-mcp/mcp-paths.json`, `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P0-provenance/mcp-help.err`
- A configured local stdio MCP server reached `status:"connected"`; OpenCode initialized it with protocol `2025-11-25` and client name/version `opencode`/`1.18.29`, then sent `notifications/initialized` and `tools/list`. — verified: OpenCode 1.18.29 — source: `/home/antst/agentbus-evidence/opencode-probes-2-20260906T175921Z/T3-mcp/mcp-status.json`, `/home/antst/agentbus-evidence/opencode-probes-2-20260906T175921Z/T3-mcp/mcp-frames-final.jsonl`
- OpenCode called MCP tool `echo_marker` once with `{"text":"MCP_CALL"}` and `_meta.progressToken:2`; the successful result was `MCP_ECHO_OK:MCP_CALL`, stored in the transcript under product tool name `probe_echo_echo_marker`, and supplied to the final assistant response. — verified: OpenCode 1.18.29 — source: `/home/antst/agentbus-evidence/opencode-probes-2-20260906T175921Z/T3-mcp/mcp-frames-final.jsonl`, `/home/antst/agentbus-evidence/opencode-probes-2-20260906T175921Z/T3-mcp/messages-final.json`
- After receiving the completed MCP tool response, OpenCode sent `notifications/cancelled` for request 2 with reason `AbortError: The operation was aborted.`; the transcript nevertheless recorded the tool call as completed. — verified: OpenCode 1.18.29 — source: `/home/antst/agentbus-evidence/opencode-probes-2-20260906T175921Z/T3-mcp/mcp-frames-final.jsonl`, `/home/antst/agentbus-evidence/opencode-probes-2-20260906T175921Z/T3-mcp/messages-final.json`

## Plugin SDK and readiness

- A globally installed config plugin at `$XDG_CONFIG_HOME/opencode/plugins/probe.js` loaded under OpenCode 1.18.29 without a project copy. — verified: OpenCode 1.18.29 / plugin SDK 1.18.29 — source: `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P7-plugin/plugin-file-installed.txt`, `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P7-plugin/plugin-events.jsonl`
- The plugin constructor input keys were `$`, `client`, `directory`, `experimental_workspace`, `project`, `serverUrl`, and `worktree`; the captured values included a usable client, project, shell, and workspace-registration function. — verified: plugin SDK 1.18.29 — source: `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P7-plugin/plugin-events.jsonl`, `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P7-plugin/index.d.ts`
- A blank OpenCode 1.18.29 TUI remained on its initial `Ask anything` screen without creating a native session, so the Sessionbus roster had no OpenCode peer at TUI start. The plugin publishes a peer only after the product emits `session.created`, which the ordinary TUI flow does at its first prompt. — verified blank state; first-prompt publication covered by P1 — source: `/home/antst/sessionbus-evidence/opencode-20260907T143438Z/real/P0/tui-pane-failure.txt`, `/home/antst/sessionbus-evidence/opencode-20260907T143438Z/real/P0/roster-initial.json`
- Reopening the TUI on an exact persisted session emitted no event naming that session during a named 60-second no-keystroke window: the legacy stream carried only `server.connected`, plugin/catalog/reference/integration updates, and heartbeats. One later title PATCH emitted `session.updated` with the exact native ID, after which exactly one OpenCode peer appeared. OpenCode therefore appears in the Sessionbus roster at its first prompt or first session change, not merely when the TUI launches. Startup only validates and passes the requested session into the TUI; status publication requires `SessionStatus.set`, while PATCH title publishes `session.updated`. — verified: OpenCode 1.18.29 runtime and source, zero turns — source: `/home/antst/sessionbus-evidence/opencode-20260907T154625Z/diagnostic/P2-resume-events-curl/event-summary.json`, `/home/antst/sessionbus-evidence/opencode-20260907T154625Z/diagnostic/P2-resume-events-curl/roster-after-load.json`, `/home/antst/sessionbus-evidence/opencode-20260907T154625Z/diagnostic/P2-resume-events-curl/roster-after-interaction.json`, `1674747:packages/opencode/src/cli/cmd/tui.ts:251-296`, `1674747:packages/opencode/src/session/status.ts:30-47`, `1674747:packages/opencode/src/session/session.ts:734-754`, `1674747:packages/opencode/src/server/routes/instance/httpapi/handlers/session.ts:183-203`
- One OpenCode 1.18.29 TUI process initialized two instance contexts for the same exact directory under run ID `117cbaf4`. The pre-fix Sessionbus plugin copied and deleted its connection environment during each instance initialization, so the second instance registered the tool but could not publish a peer for its exact `session.created`; Sessionbus-owned environment must instead be captured and scrubbed once when the module is evaluated. — verified: zero-turn runtime and accepted source diagnosis — source: `/home/antst/sessionbus-evidence/opencode-20260907T152155Z/diagnostic/P0-plugin-loader/opencode.log`, `/home/antst/sessionbus-evidence/opencode-20260907T152155Z/diagnostic/P0-plugin-loader/tool-ids.json`, `/home/antst/sessionbus-evidence/opencode-20260907T152155Z/diagnostic/P0-plugin-loader/legacy-events.raw`, `/home/antst/sessionbus-evidence/opencode-20260907T152155Z/diagnostic/P0-plugin-loader/roster.json`
- OpenCode 1.18.29 reads the same configured `plugin` array through two loaders. Its v2 loader requires a default `{id,effect}` or `{id,setup}` object and silently rejects the Sessionbus module's bare-function default; its legacy loader accepts that function and registers the `sessionbus` tool. The legacy load emits no `plugin.added` event for Sessionbus; the captured `plugin.added` rows describe the v2 plugin graph only. — verified: OpenCode 1.18.29 source and zero-turn runtime — source: `1674747:packages/core/src/config/plugin/external.ts:15-29,73-87`, `1674747:packages/opencode/src/plugin/index.ts:88-125,181-241`, `/home/antst/sessionbus-evidence/opencode-20260907T152155Z/diagnostic/P0-plugin-loader/tool-ids.json`, `/home/antst/sessionbus-evidence/opencode-20260907T152155Z/diagnostic/P0-plugin-loader/legacy-events.raw`
- The plugin `event` hook received the exact current `session.created` and `session.deleted` objects after zero-turn HTTP session operations. — verified: OpenCode 1.18.29 / plugin SDK 1.18.29 — source: `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P7-plugin/plugin-events.jsonl`
- The legacy plugin dispatcher gives an instance only events whose `event.location.directory` exactly equals that instance's directory. A TUI and its Sessionbus plugin are therefore scoped to one exact directory; session operations from another directory do not reach that plugin instance. — verified: OpenCode 1.18.29 source and zero-turn diagnostic — source: `1674747:packages/opencode/src/plugin/index.ts:251-260`, `/home/antst/sessionbus-evidence/opencode-20260907T201017Z/probe/L6-peer-trace/RESULT.txt`
- OpenCode invokes plugin event hooks without awaiting their returned promises; lifecycle events may therefore overlap while an earlier hook is awaiting product I/O. — verified: OpenCode 1.18.29 source — source: `https://github.com/anomalyco/opencode/blob/v1.18.29/packages/opencode/src/plugin/index.ts#L255-L260`
- The tool execution context exposes `sessionID`, `messageID`, `agent`, `directory`, `worktree`, `abort:AbortSignal`, metadata update, and permission asking. — verified: plugin SDK 1.18.29 — source: `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P7-plugin/tool.d.ts`
- The authenticated `/doc` response completed before the plugin constructor marker in the captured startup; HTTP readiness therefore does not prove that external plugin hooks are installed. — verified: OpenCode 1.18.29 / plugin SDK 1.18.29 — source: `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P7-plugin/app-ready-observation.json`
- The current `shell.env` hook type supplies `cwd`, optional `sessionID`, and optional `callID`; it does not supply a native title. A plugin must learn titles from session lifecycle events or an SDK read. — verified: plugin SDK 1.18.29 — source: `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P7-plugin/index.d.ts`
- An explicit zero-turn `POST /session` produced exactly one Sessionbus peer with the native ID, requested title, configured groups, and exact directory. A title PATCH then produced a same-ID rehello with the new title; deletion-hook shape is verified separately above. — verified: OpenCode 1.18.29 Sessionbus lifecycle — source: `/home/antst/sessionbus-evidence/opencode-20260907T154625Z/real/P0/peer-initial.json`, `/home/antst/sessionbus-evidence/opencode-20260907T154625Z/real/P0/peer-renamed.json`
- UNVERIFIED: child-process identity ingress on 1.18.29. The old plugin's `shell.env` hook required an exact native session ID and wrote it through two integration-specific identity variables; the current SDK type confirms optional `sessionID` exists but this mutation path was not run. — old adapter, unverified integration behavior on 1.18.29 — source: `ff81565:integrations/opencode/agent-sessions.mjs:194-204`, `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P7-plugin/index.d.ts`
- A Sessionbus delivery admitted through v2 `delivery:"steer"` to a resumed legacy TUI session executed completely in the v2 coordinator: durable sequence 32–39 and v2 history contain the exact delivered user message and stopped `OPENCODE_P2_DELIVERY_OK` assistant, but the TUI stayed on its earlier legacy transcript. The TUI server's `/event` stream did carry bridged `session.next.*` frames; that transport visibility did not project the v2 result into the selected legacy transcript. Interactive peer delivery must therefore use legacy `prompt_async`; the lane wrapper continues to use its per-session durable route as the v2 run source of truth. — verified: OpenCode 1.18.29 runtime — source: `/home/antst/sessionbus-evidence/opencode-20260907T154625Z/real/P2-announced-curl/delivery.json`, `/home/antst/sessionbus-evidence/opencode-20260907T154625Z/real/P2-announced-curl/native-durable-replay.raw`, `/home/antst/sessionbus-evidence/opencode-20260907T154625Z/real/P2-announced-curl/native-message-history.json`, `/home/antst/sessionbus-evidence/opencode-20260907T154625Z/real/P2-announced-curl/legacy-events.raw`, `/home/antst/sessionbus-evidence/opencode-20260907T154625Z/real/P2-announced-curl/tui-pane-failure.txt`
- Installing `@sessionbus/opencode` from the exact local package-directory spec twice produced byte-identical OpenCode configuration, proving the installer is idempotent for that supported form. — verified: stamped installer, zero turns — source: `/home/antst/sessionbus-evidence/opencode-20260907T134337Z/setup/RESULT.txt`, `/home/antst/sessionbus-evidence/opencode-20260907T134337Z/setup/config-after-first.sha256`, `/home/antst/sessionbus-evidence/opencode-20260907T134337Z/setup/config-after-second.sha256`

## Upstream requests

- Publish the selected native session when the TUI loads a persisted session. OpenCode already exposes a validated `tui.session.select` publication route, but the TUI startup path only validates and passes `args.session` and emits no session-bearing event until later activity. — product gap: OpenCode 1.18.29 — source: `1674747:packages/opencode/src/server/routes/instance/httpapi/handlers/tui.ts:98-105`, `1674747:packages/opencode/src/cli/cmd/tui.ts:251-296`, `/home/antst/sessionbus-evidence/opencode-20260907T154625Z/diagnostic/P2-resume-events-curl/event-summary.json`

## Probe closure

- The probe spent exactly two model turns: one terminal `prompt_async` turn and one permission turn; both were issued once without retry. — verified: probe procedure — source: `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/RUN-SUMMARY.md`
- Both probe sessions were deleted, both isolated servers exited, the event clients exited, no probe-owned process remained, and copied credentials plus server-password files were removed. — verified: probe procedure — source: `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P8-teardown/processes-after.txt`, `/home/antst/agentbus-evidence/opencode-probes-20260906T174118Z/P8-teardown/result.txt`
- The follow-up executed each of its three authorized scenarios once with no retry; its steer request itself started a separate v2 assistant loop, so the evidence contains four model-producing admissions rather than three same-turn deliveries. — verified: probe procedure and product behavior — source: `/home/antst/agentbus-evidence/opencode-probes-2-20260906T175921Z/RUN-SUMMARY.md`
- All three follow-up sessions were deleted, the server and stdio MCP child exited, no evidence-owned process remained, and copied credentials plus server-password files were removed. — verified: probe procedure — source: `/home/antst/agentbus-evidence/opencode-probes-2-20260906T175921Z/P9-teardown/processes-after.txt`, `/home/antst/agentbus-evidence/opencode-probes-2-20260906T175921Z/P9-teardown/result.txt`
- The durable-queue restart was orderly: the first server was stopped and absent before the second was started. — verified: probe procedure — source: `/home/antst/agentbus-evidence/opencode-probes-2-20260906T175921Z/T2-queue/processes-between-servers.txt`, `/home/antst/agentbus-evidence/opencode-probes-2-20260906T175921Z/T2-queue/restart-result.txt`
- The final two-scenario probe issued each scenario once. Queue promotion passed; the active v2-steer assertion remained unverified after the recorded harness failure and was not retried. Both sessions were deleted, the isolated server joined before credential cleanup, and the final relevant-process listing is empty. — verified: probe procedure — source: `/home/antst/agentbus-evidence/opencode-probes-3-20260906T180942Z/RUN-SUMMARY.md`, `/home/antst/agentbus-evidence/opencode-probes-3-20260906T180942Z/cleanup/delete-results.txt`, `/home/antst/agentbus-evidence/opencode-probes-3-20260906T180942Z/cleanup/server-stop-result.txt`, `/home/antst/agentbus-evidence/opencode-probes-3-20260906T180942Z/cleanup/processes-final-relevant.txt`
- The plugin-specifier probe created no native session or prompt and spent zero model turns; every OpenCode server and its owned tarball server joined, and the final relevant-process listing is empty. — verified: probe procedure — source: `/home/antst/sessionbus-evidence/opencode-plugin-spec-20260906T205328Z/RUN-SUMMARY.md`, `/home/antst/sessionbus-evidence/opencode-plugin-spec-20260906T205328Z/processes-final-relevant.txt`, `/home/antst/sessionbus-evidence/opencode-plugin-spec-20260906T205328Z/MANIFEST.sha256`
- The stamped OpenCode campaign closed at 12 authorized model-producing admissions. Previously sealed killed turns were never repeated; each extra admission records its reason in the cell ledger. — verified: campaign procedure — source: `/home/antst/sessionbus-evidence/opencode-20260907T191826Z/real/L6-lane/product-turn-ledger.txt`, `/home/antst/sessionbus-evidence/opencode-20260907T210509Z/real/L6-peer-resume/ADMISSION-AMEND.txt`
- Long-lived SSE readers are direct background children of the harness and are reaped by exact PID; only the interactive TUI runs under the cell-private tmux server. The failure trap snapshots native events and history before teardown, then verifies exact child PIDs absent. — verified: harness procedure — source: `/home/antst/sessionbus-evidence/opencode-20260907T205913Z/dry-run/L6-reader-ownership/RESULT.txt`, `/home/antst/sessionbus-evidence/opencode-20260907T210509Z/real/L6-peer-resume/failure-snapshot-order.txt`, `/home/antst/sessionbus-evidence/opencode-20260907T210509Z/real/L6-peer-resume/exact-pids-after.txt`
- Sessionbus peer discovery and delivery target resolution are group-scoped. A harness roster caller must share at least one exact group with the OpenCode peer; disjoint groups truthfully produce disjoint views. — verified: zero-turn connection diagnostic — source: `/home/antst/sessionbus-evidence/opencode-20260907T210509Z/real/L6-peer-resume/roster-group-contract.txt`
